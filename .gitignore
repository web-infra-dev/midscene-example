.DS_Store
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/command-line/*.json

# Midscene.js dump files
**/midscene_run/report/**
**/midscene_run/dump/
**/midscene_run/log/
playwright-report/
dist/
test-results/
.env

output/
docs/

.DS_Store

pnpm-lock.yaml

# Midscene.js dump files
**/midscene_run/midscene-report
**/midscene_run/dump-logger

# Midscene run artifacts (reports, logs, cache, summaries, tmp) — fully generated
**/midscene_run/

# Native Rstest projects emitted by `midscene emit` in the testing-framework demos
**/.rstest/

# npm lockfiles (this repo standardizes on pnpm; lockfiles are not committed)
package-lock.json
**/package-lock.json
